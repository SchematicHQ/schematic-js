name: Pull request - schematic-react

on:
  pull_request:
    paths:
      - react/**

jobs:
  build:
    name: Build
    runs-on: runs-on=${{ github.run_id }}/runner=4cpu-linux-x64/extras=s3-cache
    steps:
      - uses: runs-on/action@v2.0.3
      - uses: actions/checkout@v5.0.0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Bun install
        working-directory: ./react
        run: bun install
      - name: Build
        working-directory: ./react
        run: bun runbuild

  lint:
    name: Lint
    runs-on: runs-on=${{ github.run_id }}/runner=4cpu-linux-x64/extras=s3-cache
    steps:
      - uses: runs-on/action@v2.0.3
      - uses: actions/checkout@v5.0.0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Bun install
        working-directory: ./react
        run: bun install
      - name: Lint
        working-directory: ./react
        run: bun runlint

  test:
    name: Test
    runs-on: runs-on=${{ github.run_id }}/runner=4cpu-linux-x64/extras=s3-cache
    steps:
      - uses: runs-on/action@v2.0.3
      - uses: actions/checkout@v5.0.0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Bun install
        working-directory: ./react
        run: bun install
      - name: Test
        working-directory: ./react
        run: bun runtest
      - name: Test (React Native)
        working-directory: ./react
        run: bun runtest:reactnative
